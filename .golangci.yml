linters:
  enable-all: true
  disable:
    # deprecated
    - golint
    - scopelint
    - maligned
    - interfacer
    # don't use
    - gochecknoglobals
    - godot
    - gochecknoinits
    - exhaustivestruct
